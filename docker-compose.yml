version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    environment:
      - SECRET_KEY=dev_secret_key
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
